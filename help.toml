# Context-sensitive help messages, spoken when F1 is pressed on a control.
#
# This file is round-tripped by the `gen-help` dev tool:
#   cargo run --bin gen-help
# It rescans the UI source for `help::tag(..)` calls, adds any new controls
# (with a blank `message` for you to fill in), refreshes each control's `label`
# and `file`, and preserves everything you have already written. Controls that no
# longer exist in the source are moved to a `[[stale]]` section rather than
# deleted, so a rename never loses your text.
#
# Fill in `message` for each control. A blank message falls back to a generic
# "No help available for this control." at runtime. `label` and `file` are
# context for you and are ignored by the app.

[[control]]
id = "dialog.appPicker.list"
label = "Running applications list"
file = "src/ui/app_picker.rs"
message = "Arrow up and down through the applications you can capture. Each entry gives the application's name followed by its program file. Press ENTER or use the Select button to capture the highlighted one."

[[control]]
id = "dialog.appPicker.refresh"
label = "Refresh the application list button"
file = "src/ui/app_picker.rs"
message = "Look for applications again. Use this after starting an application, or after playing something in one, so that it shows up in the list."

[[control]]
id = "dialog.appPicker.select"
label = "Select highlighted application button"
file = "src/ui/app_picker.rs"
message = "Capture the application highlighted in the list and close this dialog."

[[control]]
id = "dialog.appPicker.soundOnly"
label = "Only show apps that have played sound checkbox"
file = "src/ui/app_picker.rs"
message = "When checked, the list only offers applications that have played sound since they started, which is usually a much shorter list. Uncheck it to see every open application, including ones that have not made a sound yet."

[[control]]
id = "dialog.appPicker.typeName"
label = "Type an application name button"
file = "src/ui/app_picker.rs"
message = "Type a program name by hand, for an application that is not running yet. The source stays silent until that program starts, then picks it up on its own."

[[control]]
id = "dialog.chatView.text"
label = "Full chat message text"
file = "src/ui/chat.rs"
message = "Arrow up and down through this list to view chats. Home jumps to the oldest message while END jumps to the most recent one."

[[control]]
id = "dialog.connect.addServiceType"
label = "New service type selector"
file = "src/ui/connect_dialog.rs"
message = "Choose whether the new service profile is an Audiopub service or a direct Icecast service."

[[control]]
id = "dialog.connect.addSite"
label = "Add service button"
file = "src/ui/connect_dialog.rs"
message = "Add a new streaming service profile. Choose Audiopub for an Audiopub site, or Icecast for direct source streaming."

[[control]]
id = "dialog.connect.connectButton"
label = "Connect or disconnect button"
file = "src/ui/connect_dialog.rs"
message = "Connects to the selected service. Audiopub services log in with the URL, email, and password. Icecast services are armed from the saved server, mount, username, and password, then connect to Icecast when you start streaming. Once connected, this button changes to Disconnect."

[[control]]
id = "dialog.connect.email"
label = "Email address for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the email address you use to log in to the selected Audiopub service."

[[control]]
id = "dialog.connect.icecastMount"
label = "Icecast mount point for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the Icecast mount point for this service. A leading slash is optional."

[[control]]
id = "dialog.connect.icecastPassword"
label = "Icecast password for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the Icecast source password or stream key for this service."

[[control]]
id = "dialog.connect.icecastPort"
label = "Icecast port for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the Icecast port number. The default is 8000."

[[control]]
id = "dialog.connect.icecastServer"
label = "Icecast server for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the Icecast server host or address, without the port."

[[control]]
id = "dialog.connect.icecastUsername"
label = "Icecast username for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the Icecast source username. If left blank, Pubsplash uses source."

[[control]]
id = "dialog.connect.nickname"
label = "Streaming service nickname"
file = "src/ui/connect_dialog.rs"
message = "Type a short name for this service as it should appear in the services list."

[[control]]
id = "dialog.connect.password"
label = "Password for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the password you use to log in to the selected Audiopub service."

[[control]]
id = "dialog.connect.removeSite"
label = "Remove service button"
file = "src/ui/connect_dialog.rs"
message = "This deletes the selected service profile. The main Audiopub service cannot be removed."

[[control]]
id = "dialog.connect.renameSite"
label = "Rename service button"
file = "src/ui/connect_dialog.rs"
message = "Rename the selected service profile. The main Audiopub service cannot be renamed."

[[control]]
id = "dialog.connect.serviceType"
label = "Service type selector"
file = "src/ui/connect_dialog.rs"
message = "Choose whether the selected non-main service is Audiopub or Icecast. The main Audiopub service shows this selector disabled because its type is fixed."

[[control]]
id = "dialog.connect.siteList"
label = "Configured streaming services list"
file = "src/ui/connect_dialog.rs"
message = "Use the arrow keys to select a streaming service, then tab to its settings. Audiopub is already here for you; add more Audiopub or Icecast services with the Add service button."

[[control]]
id = "dialog.connect.url"
label = "Audiopub URL for the selected service"
file = "src/ui/connect_dialog.rs"
message = "Type the full Audiopub service URL, starting with http or https. The main Audiopub service keeps its built-in URL."

[[control]]
id = "dialog.fxEditor.bypass"
label = "Bypass this plugin checkbox"
file = "src/ui/fx_editor.rs"
message = "When checked, the selected plugin is bypassed, and will not output anything."

[[control]]
id = "dialog.fxEditor.close"
label = "Close plugin interface button"
file = "src/ui/fx_editor.rs"
message = "This will close the native interface of the selected VST plugin."

[[control]]
id = "dialog.fxEditor.focusPlugin"
label = "Move focus into the plugin's own interface button"
file = "src/ui/fx_editor.rs"
message = "This attempts to focus the plugin's native interface."

[[control]]
id = "dialog.fxEditor.parameters"
label = "Open accessible parameters dialog button"
file = "src/ui/fx_editor.rs"
message = "Opens a dialog that allows editing of all the plugin's parameters it exposes to automation."

[[control]]
id = "dialog.fxParams.filter"
label = "Filter parameters by name box"
file = "src/ui/fx_params.rs"
message = "Filters parameters by a given name. For instance, if you're using a voice doubler, and you can pan each of the cloned voices, typing pan into the filter box would show only parameters whose name contains pan."

[[control]]
id = "dialog.fxParams.parameter"
label = "Plugin parameter chooser"
file = "src/ui/fx_params.rs"
message = "Use arrows and first-letter navigation to navigate to a parameter, then press tab to enter the slider for menipulating it."

[[control]]
id = "dialog.fxParams.showUnnamed"
label = "Show unnamed parameters checkbox"
file = "src/ui/fx_params.rs"
message = "Show or hide unnamed parameters."

[[control]]
id = "dialog.fxParams.value"
label = "Selected parameter value slider"
file = "src/ui/fx_params.rs"
message = "Use arrow keys as well as page up, page down, home and end to adjust the parameter. You should hear real values as reported by the plugin where possible, for instance, 300ms for a delay value."

[[control]]
id = "dialog.fxParams.valueText"
label = "Type-in value entry for the selected parameter"
file = "src/ui/fx_params.rs"
message = ""

[[control]]
id = "dialog.keybind.action"
label = "Binding action choice"
file = "src/ui/keybinds_ui.rs"
message = "What the shortcut will do. Some actions need to know which scene, source or bus they apply to; choosing one of those adds a further list right after this one."

[[control]]
id = "dialog.keybind.category"
label = "Binding category choice"
file = "src/ui/keybinds_ui.rs"
message = "Which group of actions to choose from: streaming, scenes, monitoring or muting. Changing this refills the action list below it."

[[control]]
id = "dialog.keybind.global"
label = "Global binding checkbox"
file = "src/ui/keybinds_ui.rs"
message = "When checked, this shortcut works anywhere in Windows, not only while Pubsplash is in front, unless some other application swallows it first. A global shortcut must include CTRL, ALT or SHIFT, or be a function key: on its own a plain letter or digit would be taken away from everything you type in."

[[control]]
id = "dialog.keybind.shortcut"
label = "Shortcut capture field"
file = "src/ui/keybinds_ui.rs"
message = "While this field has focus, press the key combination you want and it is read back to you. ESCAPE or DELETE clears it again. TAB and SHIFT plus TAB are left alone so you can always move on, which means they cannot themselves be bound. F1 and F6 are refused as well: F1 speaks this help and F6 moves between the lists on a tab."

[[control]]
id = "dialog.keybind.specifier"
label = "Binding scene, source, bus or media player choice"
file = "src/ui/keybinds_ui.rs"
message = "Which scene, source, bus or media player the action applies to. This appears only for actions that need one, and disappears again when you pick an action that does not. A source shortcut runs against the scene that is live when you press it, so it does nothing while you are in a scene that has no source by that name."

[[control]]
id = "dialog.loadChain.delete"
label = "Delete selected chain button"
file = "src/ui/buses.rs"
message = "Remove the selected FX chain."

[[control]]
id = "dialog.loadChain.list"
label = "Saved FX chains list"
file = "src/ui/buses.rs"
message = "A list of saved FX chains you can apply to the selected bus."

[[control]]
id = "dialog.loadChain.load"
label = "Load selected chain button"
file = "src/ui/buses.rs"
message = "Load the selected FX chain and apply it to the bus you're working with."

[[control]]
id = "dialog.mastodonAuth.status"
label = "Mastodon authorization status"
file = "src/ui/mastodon_post.rs"
message = "Says what the authorization is waiting for. Pubsplash opens your browser so you can sign in to your Mastodon server and approve it; this window closes on its own once you have. Use Cancel, or press ESCAPE, to give up."

[[control]]
id = "dialog.mastodonResume.help"
label = "Token help button on the resumed stream post"
file = "src/ui/mastodon_templates.rs"
message = "Lists the tokens you can put in this post and what each one is replaced with."

[[control]]
id = "dialog.mastodonResume.text"
label = "Resumed stream post"
file = "src/ui/mastodon_templates.rs"
message = "What to post about resuming this stream. It starts out filled in and selected, so you can type straight over it. Tokens in curly braces work here exactly as they do in a saved template; press Help for the list. The hashtag PubsplashStreamInfo is added to the end for you."

[[control]]
id = "dialog.mastodonTemplate.help"
label = "Token help button"
file = "src/ui/mastodon_templates.rs"
message = "Lists the tokens you can put in a template and what each one is replaced with."

[[control]]
id = "dialog.mastodonTemplate.kind"
label = "Announcement type selector"
file = "src/ui/mastodon_templates.rs"
message = "Choose when this template is used. Start of stream templates go out when a stream begins; stream continuation templates are the ones used for the periodic still-streaming posts. If you have written several of a type, Pubsplash picks one of them at random each time."

[[control]]
id = "dialog.mastodonTemplate.text"
label = "Template text input"
file = "src/ui/mastodon_templates.rs"
message = "The wording of the announcement. Put a token in curly braces anywhere you want the real value, for example: I am live at {url}. Press Help for the list of tokens. If a token is misspelled, or a brace is left unclosed, you are told which one when you choose OK and brought straight back here with your text still in place. The hashtag PubsplashStreamInfo is added to the end of every post for you."

[[control]]
id = "dialog.mastodonTokens.text"
label = "Template token reference"
file = "src/ui/mastodon_templates.rs"
message = "The tokens you can use in a template, one per line, each with what it is replaced with. You can select text here and copy it with CONTROL plus C."

[[control]]
id = "dialog.mediaPlayerSource.calibrate"
label = "Calibrate ducking threshold button"
file = "src/ui/scenes.rs"
message = "Sets the level above for you. Press this and talk normally for five seconds, at the volume you actually broadcast at, and Pubsplash measures the very signal the ducking watches, then puts the level a little way underneath what it heard. It listens to the sources in your live scene, through their volume sliders and mutes, so the microphone you are calibrating has to be in that scene and unmuted. When it has finished it tells you the level it chose, and the slider above moves to it. If nothing loud enough to be a voice was heard, it says so and nothing is changed. Nothing is saved until you choose OK."

[[control]]
id = "dialog.mediaPlayerSource.duck"
label = "Ducking checkbox"
file = "src/ui/scenes.rs"
message = "When checked, the music turns itself down whenever any other source in this scene is making sound, and comes back up about a second after it stops. Your microphone and your text-to-speech are both other sources, so this is what lets you talk over the music without touching a fader. Making a sound means a real signal rather than the hiss of an open microphone in a quiet room, so an idle microphone does not hold the music down. Other media players do not count either, so two of them never fight each other. The level it drops to is the next control."

[[control]]
id = "dialog.mediaPlayerSource.duckLevel"
label = "Turned-down level slider"
file = "src/ui/scenes.rs"
message = "How loud the music is while it is turned down, as a percentage of this source's own volume slider. Twenty-five percent means a quarter of whatever the fader is set to, which leaves the music audible underneath you. Zero silences it completely while anything else is playing. This has no effect unless the checkbox above is checked."

[[control]]
id = "dialog.mediaPlayerSource.duckThreshold"
label = "Ducking threshold slider"
file = "src/ui/scenes.rs"
message = "How loud another source has to get before the music is turned down, in decibels below full scale. Minus sixty is almost anything, minus ten is only a shout, and the default of minus thirty is the level of somebody deliberately talking, far enough above a breath across the microphone or a knock on the desk that neither turns your music down. Lower it if a normal sentence does not duck the music, raise it if something in the room does, or press the Calibrate button below and let Pubsplash measure your own voice. This has no effect unless the ducking checkbox is checked."

[[control]]
id = "dialog.mediaPlayerSource.folder"
label = "Media player folder box"
file = "src/ui/scenes.rs"
message = "The folder this source plays, including everything in its subfolders. Type the full path or use the browse button. Pubsplash plays MP3, M4A, AAC, FLAC, OGG, WAV, AIFF and a few others; anything it cannot decode, such as Opus or WMA, is ignored. The folder is read again every few minutes, so music you add during a session joins in without restarting anything. Until this is filled in, the source stays silent."

[[control]]
id = "dialog.mediaPlayerSource.shuffle"
label = "Shuffle checkbox"
file = "src/ui/scenes.rs"
message = "When checked, the folder is played in a random order, and every file is played once before any of them repeats. It is reshuffled each time it runs out, and never starts a new round with the track that just finished. Unchecked plays the folder in filename order instead, over and over."

[[control]]
id = "dialog.missingPlugins.apply"
label = "Apply chain with available plugins button"
file = "src/ui/buses.rs"
message = "If Pubsplash determines that you don't have all of the plugins this chain uses, you can still apply the chain, minus those plugins."

[[control]]
id = "dialog.missingPlugins.list"
label = "Missing plugins list"
file = "src/ui/buses.rs"
message = "Shows the list of plugins you lack to apply this FX chain in full."

[[control]]
id = "dialog.preferences.archive.archiveDefault"
label = "Archive streams by default checkbox"
file = "src/ui/preferences.rs"
message = "When this box is checked, streams will be archived on Audiopub by default. You can still uncheck the box under file > set stream info if you don't want a particular stream archived."

[[control]]
id = "dialog.preferences.archive.browse"
label = "Browse for recording folder button"
file = "src/ui/preferences.rs"
message = "This brings up a standard windows file browser that allows you to select the folder that Pubsplash will save your local recordings to."

[[control]]
id = "dialog.preferences.archive.recordDefault"
label = "Record streams by default checkbox"
file = "src/ui/preferences.rs"
message = "When this box is checked, streams will automatically be recorded and saved to disk. You can still uncheck the box under file > set stream info if you don't want a particular stream to be recorded."

[[control]]
id = "dialog.preferences.archive.recordingFolder"
label = "Recording folder path input"
file = "src/ui/preferences.rs"
message = "Type the path where you'd like your local recordings to be saved to, or use the browse button to pick a folder using a standard Windows folder picker."

[[control]]
id = "dialog.preferences.general.checkNow"
label = "Check for updates now button"
file = "src/ui/preferences.rs"
message = "Asks GitHub straight away whether a newer version of Pubsplash exists, without waiting for the next time you start the app. Unlike the automatic check, this one always answers: if you already have the newest version, it says so. If there is a newer one, Pubsplash asks whether you'd like it before downloading anything."

[[control]]
id = "dialog.preferences.general.checkOnStart"
label = "Check for updates on startup checkbox"
file = "src/ui/preferences.rs"
message = "When this box is checked, Pubsplash asks GitHub once each time it starts whether a newer version exists. If there isn't one, or GitHub can't be reached, nothing is said at all. If there is, Pubsplash asks whether you'd like to download and install it; nothing is downloaded or replaced unless you say yes. The check is skipped entirely while you are streaming or recording."

[[control]]
id = "dialog.preferences.keybinds.add"
label = "Add binding button"
file = "src/ui/keybinds_ui.rs"
message = "Opens a dialog where you choose an action and press the keys you want to run it with."

[[control]]
id = "dialog.preferences.keybinds.edit"
label = "Edit binding button"
file = "src/ui/keybinds_ui.rs"
message = "Opens the same dialog for the action highlighted in the list, filled in with whatever it is bound to now, so you can change the keys or make the shortcut global."

[[control]]
id = "dialog.preferences.keybinds.list"
label = "Keybinds list"
file = "src/ui/keybinds_ui.rs"
message = "Every action you can give a keyboard shortcut to. Arrow up and down through them. Each row gives the action, then the keys bound to it, or Unassigned when it has none, and says global when the shortcut works even while another application is in front. Press DELETE, or use the remove binding button, to take a shortcut away. Streaming starts and stops with F9 and recording with F10 until you change them."

[[control]]
id = "dialog.preferences.keybinds.remove"
label = "Remove binding button"
file = "src/ui/keybinds_ui.rs"
message = "Takes the shortcut away from the highlighted action, leaving the action itself alone. The DELETE key in the list does the same thing."

[[control]]
id = "dialog.preferences.keybinds.reset"
label = "Reset keybinds to defaults button"
file = "src/ui/keybinds_ui.rs"
message = "Throws away every shortcut you have made and puts back the two Pubsplash ships with, F9 for streaming and F10 for recording. You are asked to confirm first."

[[control]]
id = "dialog.preferences.logging.compress"
label = "Compress logs button"
file = "src/ui/logging_ui.rs"
message = "Packages the log files and any crash reports into a single ZIP file, and asks you where to save it with a standard Windows save box. This is the file to attach when you report a problem. Pubsplash stops logging for the moment it takes, so that the log of the session you are in right now is closed off and goes into the archive complete rather than with its last few lines missing; logging carries straight on afterwards. Nothing else is included: no passwords, no API keys, and none of your settings."

[[control]]
id = "dialog.preferences.logging.level"
label = "Log level choice"
file = "src/ui/logging_ui.rs"
message = "How much detail Pubsplash writes to its log file. Info, the setting it ships with, records the things that matter: what connected, what failed, what you started and stopped. Debug and trace add a great deal more, and are worth turning on when you are chasing a problem and have been asked for a log; trace in particular writes constantly while audio is running, so put it back to info afterwards. Error and warn record less than info, and off writes nothing at all. The change takes effect at once, and is remembered. If a PUBSPLASH_LOG environment variable is set, it fixes the level for the run and this box is disabled."

[[control]]
id = "dialog.preferences.logging.openFolder"
label = "Open logs folder button"
file = "src/ui/logging_ui.rs"
message = "Opens the folder the log files are written to in Windows Explorer. The newest one is called pubsplash_rCURRENT.log and is the one being written to right now; the numbered ones beside it are older. If you are collecting a log to send to somebody, the Compress logs button is the easier way, since it gathers all of them and the crash reports in one go."

[[control]]
id = "dialog.preferences.mastodon.addTemplate"
label = "Add template button"
file = "src/ui/mastodon_prefs.rs"
message = "Write a new announcement template. You choose whether it is for the start of a stream or for the still-streaming posts, then type the wording."

[[control]]
id = "dialog.preferences.mastodon.authorize"
label = "Authorize with Mastodon button"
file = "src/ui/mastodon_prefs.rs"
message = "Link your Mastodon account. Pubsplash opens your browser at the server named above so you can sign in and approve it, then stores the authorization. Nothing is posted until you also turn an announcement on."

[[control]]
id = "dialog.preferences.mastodon.editTemplate"
label = "Edit template button"
file = "src/ui/mastodon_prefs.rs"
message = "Change the template highlighted in the list."

[[control]]
id = "dialog.preferences.mastodon.interval"
label = "Still-streaming post interval choice"
file = "src/ui/mastodon_prefs.rs"
message = "How long to wait between still-streaming posts. The clock starts when the stream goes live, so on the hourly setting the first one goes out an hour after you start. Available only while still-streaming posts are turned on."

[[control]]
id = "dialog.preferences.mastodon.periodic"
label = "Make periodic still-streaming Mastodon posts checkbox"
file = "src/ui/mastodon_prefs.rs"
message = "When checked, Pubsplash keeps posting a reminder that you are still on the air while a stream runs, using the interval in the list after this box. This only sets the starting state of the matching box in the Set stream info dialog; you can still turn it off for one stream there."

[[control]]
id = "dialog.preferences.mastodon.postOnStart"
label = "Post to Mastodon when I start a new stream checkbox"
file = "src/ui/mastodon_prefs.rs"
message = "When checked, Pubsplash posts to Mastodon once a new stream is live. This only sets the starting state of the matching box in the Set stream info dialog; you can still turn it off for one stream there."

[[control]]
id = "dialog.preferences.mastodon.removeTemplate"
label = "Remove template button"
file = "src/ui/mastodon_prefs.rs"
message = "Delete the template highlighted in the list. You can also press DELETE in the list. Deleting all of them is safe: Pubsplash falls back on its own wording."

[[control]]
id = "dialog.preferences.mastodon.server"
label = "Mastodon server input"
file = "src/ui/mastodon_prefs.rs"
message = "The Mastodon server your account is on, such as mastodon dot social. Type just the host name; Pubsplash works out the rest. Fill this in before choosing Authorize."

[[control]]
id = "dialog.preferences.mastodon.status"
label = "Linked Mastodon account"
file = "src/ui/mastodon_prefs.rs"
message = "Which account is linked, if any. Read-only."

[[control]]
id = "dialog.preferences.mastodon.templateList"
label = "Mastodon announcement templates list"
file = "src/ui/mastodon_prefs.rs"
message = "The announcement templates you have written. Each row gives the type, a colon, and the wording. Start of stream templates come first, then the stream continuation ones. Arrow up and down to choose one, then use Edit or Remove. If you have more than one of a type, Pubsplash picks one of them at random each time it posts."

[[control]]
id = "dialog.preferences.mastodon.unlink"
label = "Unlink the Mastodon account button"
file = "src/ui/mastodon_prefs.rs"
message = "Forget the linked Mastodon account. Pubsplash stops posting and asks the server to cancel the authorization. Available only while an account is linked."

[[control]]
id = "dialog.preferences.sounds.importPack"
label = "Import sound pack button"
file = "src/ui/preferences.rs"
message = "Opens a file dialog to pick a compiled sound pack, a dot pspack file. Pubsplash keeps its own copy, so you can move or delete the file you picked afterwards, and the pack is added to the sound pack list and becomes the one in use."

[[control]]
id = "dialog.preferences.sounds.pack"
label = "Sound pack choice"
file = "src/ui/preferences.rs"
message = "The sound pack every cue plays from: the startup and shut-down sounds, and every Sound Events source. Built-in default is the pack that ships inside Pubsplash; anything else you have imported is listed after it. Arrow through the list freely, the pack you land on is loaded a moment after you stop, or straight away if you tab out of the list."

[[control]]
id = "dialog.preferences.sounds.playShutdown"
label = "Play the shut-down sound checkbox"
file = "src/ui/preferences.rs"
message = "When checked, Pubsplash plays a short sound as it closes, and waits for it to finish before the window goes away. Uncheck it for a silent, immediate exit. Only you hear this sound; it is never sent to the stream."

[[control]]
id = "dialog.preferences.sounds.playStartup"
label = "Play the startup sound checkbox"
file = "src/ui/preferences.rs"
message = "When checked, Pubsplash plays a short sound as it launches, so you know it is loading. Only you hear this sound; it is never sent to the stream."

[[control]]
id = "dialog.preferences.sounds.removePack"
label = "Remove sound pack button"
file = "src/ui/preferences.rs"
message = "Deletes Pubsplash's copy of the sound pack chosen in the list, after asking you to confirm. The cues go back to the built-in default pack. The dot pspack file you imported from is left alone. This button is unavailable while Built-in default is chosen."

[[control]]
id = "dialog.preferences.speech.awsId"
label = "AWS access key ID"
file = "src/ui/preferences.rs"
message = "Enter a draft AWS access key ID together with the secret and region, then press Validate. Both credentials are saved only after Polly discovery succeeds."

[[control]]
id = "dialog.preferences.speech.awsRegion"
label = "AWS region"
file = "src/ui/preferences.rs"
message = "The AWS region to send Polly requests to, for example us-east-1."

[[control]]
id = "dialog.preferences.speech.awsSecret"
label = "AWS secret access key"
file = "src/ui/preferences.rs"
message = "Your AWS secret access key. It is encrypted for your Windows account before being saved."

[[control]]
id = "dialog.preferences.speech.azureKey"
label = "Azure subscription key"
file = "src/ui/preferences.rs"
message = "Type or paste a draft Azure Speech key and region, then press Validate. Both are saved only after Azure returns its voice catalog successfully."

[[control]]
id = "dialog.preferences.speech.azureRegion"
label = "Azure region"
file = "src/ui/preferences.rs"
message = "The Azure region your Speech resource lives in, for example eastus. Azure will not answer without it."

[[control]]
id = "dialog.preferences.speech.elevenlabsKey"
label = "ElevenLabs API key"
file = "src/ui/preferences.rs"
message = "Type or paste a draft ElevenLabs API key, then press Validate. Failed validation or closing Preferences keeps the last validated key and cached catalog."

[[control]]
id = "dialog.preferences.speech.engine"
label = "Speech engine choice"
file = "src/ui/preferences.rs"
message = "Choose which speech engine's settings to fill in. Everything below this box belongs to the engine named here, so you only tab through the one you are working on. SAPI 5, Microsoft Edge and Google Translate need nothing filled in. This is only about entering credentials; you pick the engine a source actually speaks with in that source's own dialog, on the Scenes tab. Pubsplash reopens this tab on whichever engine you were last looking at."

[[control]]
id = "dialog.preferences.speech.googleKey"
label = "Google Cloud API key"
file = "src/ui/preferences.rs"
message = "Type or paste a draft Google Cloud Text to Speech API key, then press Validate. It is saved only after Google returns its voice catalog successfully."

[[control]]
id = "dialog.preferences.speech.maxChars"
label = "Maximum message length"
file = "src/ui/preferences.rs"
message = "The longest chat message that will be spoken. Longer messages are cut short rather than skipped. This keeps one very long message from tying up the speech engine, and keeps costs down on the paid services."

[[control]]
id = "dialog.preferences.speech.minInterval"
label = "Minimum request interval"
file = "src/ui/preferences.rs"
message = "The shortest gap between two requests to a speech service, in milliseconds. Raising it spreads out a burst of chat messages so a busy stream does not run up a bill or hit a rate limit."

[[control]]
id = "dialog.preferences.speech.openaiKey"
label = "OpenAI API key"
file = "src/ui/preferences.rs"
message = "Type or paste a draft OpenAI API key, then press Validate. The saved key and voice catalog change only after OpenAI accepts the draft; closing Preferences or a failed validation keeps the last validated key."

[[control]]
id = "dialog.preferences.speech.starHost"
label = "Star server URL"
file = "src/ui/preferences.rs"
message = "The WebSocket address of your Star coagulator, for example ws colon slash slash localhost colon 4567."

[[control]]
id = "dialog.preferences.vst.addFolder"
label = "Add plugin folder button"
file = "src/ui/preferences.rs"
message = "Allows adding a folder to the list of those that will be scanned for plugins when you press the scan or rescan buttons."

[[control]]
id = "dialog.preferences.vst.folderList"
label = "VST plugin folders list"
file = "src/ui/preferences.rs"
message = "Use the arrow keys to review this list of folders. If you have plugins that live in other folders, you may use the add button to add them to this list, then initiate a scan to discover them."

[[control]]
id = "dialog.preferences.vst.removeFolder"
label = "Remove plugin folder button"
file = "src/ui/preferences.rs"
message = "Removes a folder from the list of folders that will be scanned for VST plugins."

[[control]]
id = "dialog.preferences.vst.rescanAll"
label = "Rescan all plugins button"
file = "src/ui/preferences.rs"
message = "Discards the plugin cache and scans every folder in the list for plugins. If successful, a cache is written and the plugins become usable on the buses tab of the main interface."

[[control]]
id = "dialog.preferences.vst.scanNew"
label = "Scan for new plugins button"
file = "src/ui/preferences.rs"
message = "Scans the list of folders for plugins, adding only those that Pubsplash didn't know about before. Useful if you've recently installed plugins."

[[control]]
id = "dialog.scan.cancel"
label = "Cancel the scan button"
file = "src/ui/scan_dialog.rs"
message = "Stops the scan and throws away everything it found; the plugins you already had are left as they were. ESCAPE does the same. If a plugin is being loaded right now, the scan stops as soon as that one finishes or is skipped, so this can take a moment to take effect."

[[control]]
id = "dialog.scan.progress"
label = "Scan progress bar"
file = "src/ui/scan_dialog.rs"
message = "How far through the scan Pubsplash is, as a percentage. It stays at zero until the folders have been searched and the number of plugins is known."

[[control]]
id = "dialog.scan.skip"
label = "Skip the plugin being scanned button"
file = "src/ui/scan_dialog.rs"
message = "Gives up on the plugin named in the status text and moves on to the next one. ENTER does the same. Use this when a plugin is taking so long that you don't think it will finish. The skipped plugin is recorded as unusable and will not be tried again until you rescan all plugins."

[[control]]
id = "dialog.scan.status"
label = "Scan status text"
file = "src/ui/scan_dialog.rs"
message = "What the scan is doing right now: how many plugins are done out of how many, and the name of the one being loaded. Read-only, and it does not read itself out as it changes, so tab here whenever you want to know where the scan has got to."

[[control]]
id = "dialog.sends.add"
label = "Add a send to a bus button"
file = "src/ui/sends.rs"
message = "Add a send to the selected bus."

[[control]]
id = "dialog.sends.level"
label = "Send level slider for the selected bus"
file = "src/ui/sends.rs"
message = "How much of the output of the effect would you like to send to the bus."

[[control]]
id = "dialog.sends.remove"
label = "Remove selected send button"
file = "src/ui/sends.rs"
message = "Remove the selected send from the bus."

[[control]]
id = "dialog.sends.sendList"
label = "Bus sends for this source list"
file = "src/ui/sends.rs"
message = "Use the arrow keys to select a bus to work with."

[[control]]
id = "dialog.sends.toMaster"
label = "Send this source directly to master checkbox"
file = "src/ui/sends.rs"
message = "When checked, the source's output is sent to master as well as to the selected bus. This lets you, for instance, set up a reverb so that you still hear the dry signal, but can lower the send level and give the reverb effect the full wet signal."

[[control]]
id = "dialog.soundEventsSource.incomingChat"
label = "Incoming chat message checkbox"
file = "src/ui/scenes.rs"
message = "When checked, this source plays a sound each time a chat message arrives from a listener."

[[control]]
id = "dialog.soundEventsSource.listenerDecrease"
label = "Listener count decreased checkbox"
file = "src/ui/scenes.rs"
message = "When checked, this source plays a sound each time a listener leaves your stream."

[[control]]
id = "dialog.soundEventsSource.listenerIncrease"
label = "Listener count increased checkbox"
file = "src/ui/scenes.rs"
message = "When checked, this source plays a sound each time a new listener joins your stream."

[[control]]
id = "dialog.soundEventsSource.listenerPeakIncrease"
label = "Listener peak increased checkbox"
file = "src/ui/scenes.rs"
message = "When checked, this source plays a sound whenever your stream reaches a new record for the most listeners at once."

[[control]]
id = "dialog.soundEventsSource.outgoingChat"
label = "Outgoing chat message checkbox"
file = "src/ui/scenes.rs"
message = "When checked, this source plays a sound each time you send a chat message of your own."

[[control]]
id = "dialog.soundEventsSource.toStream"
label = "Send these sounds to the stream checkbox"
file = "src/ui/scenes.rs"
message = "This only controls whether your listeners hear these cues; you always hear them yourself either way. Checked by default, so the cues are mixed into the stream along with playing on your own speakers. Uncheck it to keep them to yourself: they then never reach the stream or a recording. Note that the copy you hear bypasses the mixer, so this source's volume slider only affects what your listeners hear, though muting the source silences the cues everywhere."

[[control]]
id = "dialog.streamInfo.archive"
label = "Archive the stream checkbox"
file = "src/ui/stream_info_dialog.rs"
message = "When this is checked, your stream will be archived to Audiopub and posted after it ends. If you'd like to control whether this happens automatically for new streams, visit the archive tab under preferences."

[[control]]
id = "dialog.streamInfo.description"
label = "Stream description input"
file = "src/ui/stream_info_dialog.rs"
message = "Type a description for your live stream which appears under the title on Audiopub."

[[control]]
id = "dialog.streamInfo.mastodonPeriodic"
label = "Post periodic still-streaming announcements checkbox"
file = "src/ui/stream_info_dialog.rs"
message = "When checked, Pubsplash posts a still-streaming reminder to Mastodon at the interval set in Preferences, Mastodon, for as long as this stream runs. Available only when a Mastodon account is linked."

[[control]]
id = "dialog.streamInfo.mastodonStart"
label = "Post to Mastodon when this stream starts checkbox"
file = "src/ui/stream_info_dialog.rs"
message = "When checked, Pubsplash posts to Mastodon once this stream is live. It waits until the stream is really up, so the link in the post always works. Available only when a Mastodon account is linked."

[[control]]
id = "dialog.streamInfo.quality"
label = "Stream audio quality (bitrate) choice"
file = "src/ui/stream_info_dialog.rs"
message = "Set the audio quality of your stream, a higher number means higher quality. 128kbps is probably as low as you'd ever want to go with MP3."

[[control]]
id = "dialog.streamInfo.record"
label = "Record this stream locally checkbox"
file = "src/ui/stream_info_dialog.rs"
message = "When this is checked, your stream will be recorded and saved to disk. If you'd like to control whether this happens automatically for new streams, as well as customize where these recordings are saved, visit the archive tab under preferences."

[[control]]
id = "dialog.streamInfo.title"
label = "Stream title input"
file = "src/ui/stream_info_dialog.rs"
message = "Type the title of your stream here."

[[control]]
id = "dialog.ttsSource.azureDegree"
label = "Azure style intensity"
file = "src/ui/scenes.rs"
message = "Set how strongly Azure applies the selected speaking style. One is the voice's normal intensity; the allowed range is zero point zero one through two. This control is unavailable while Default speaking style is selected."

[[control]]
id = "dialog.ttsSource.azureRole"
label = "Azure speaking role"
file = "src/ui/scenes.rs"
message = "Choose an optional character role supported by the selected Azure voice. Default speaking role omits the role and leaves the voice unchanged."

[[control]]
id = "dialog.ttsSource.azureStyle"
label = "Azure speaking style"
file = "src/ui/scenes.rs"
message = "Choose an expressive style reported by the selected Azure voice, such as cheerful or chat. Default speaking style omits the style setting."

[[control]]
id = "dialog.ttsSource.elevenBoost"
label = "ElevenLabs speaker boost"
file = "src/ui/scenes.rs"
message = "Choose whether ElevenLabs speaker boost is left at the provider default, explicitly turned on, or explicitly turned off. This setting is unavailable for Eleven version 3."

[[control]]
id = "dialog.ttsSource.elevenLanguage"
label = "ElevenLabs language code"
file = "src/ui/scenes.rs"
message = "Optionally enter a two-letter language code, such as en or fr, to enforce pronunciation for models that support it. Leave blank to omit the parameter."

[[control]]
id = "dialog.ttsSource.elevenModel"
label = "ElevenLabs model"
file = "src/ui/scenes.rs"
message = "Choose an ElevenLabs model discovered with your validated credentials. A saved model that is no longer reported remains available as an unavailable legacy choice."

[[control]]
id = "dialog.ttsSource.elevenSimilarity"
label = "ElevenLabs similarity boost"
file = "src/ui/scenes.rs"
message = "When Override similarity boost is checked, set how closely the output should resemble the original voice, from zero through one. It is unavailable for Eleven version 3."

[[control]]
id = "dialog.ttsSource.elevenStability"
label = "ElevenLabs stability"
file = "src/ui/scenes.rs"
message = "When Override stability is checked, set consistency from zero through one. Lower values are more expressive and variable; higher values are more consistent."

[[control]]
id = "dialog.ttsSource.elevenStream"
label = "Stream ElevenLabs audio"
file = "src/ui/scenes.rs"
message = "When checked, speech begins playing as ElevenLabs generates it instead of waiting for the whole message, which noticeably shortens the delay before a chat message is read. Uncheck it to wait for the complete clip. It is unavailable for Eleven version 3, which has no streaming endpoint."

[[control]]
id = "dialog.ttsSource.elevenStyle"
label = "ElevenLabs style exaggeration"
file = "src/ui/scenes.rs"
message = "When Override style exaggeration is checked, set the strength of the voice's learned style from zero through one. Leave the override unchecked to omit this parameter."

[[control]]
id = "dialog.ttsSource.engine"
label = "TTS engine choice"
file = "src/ui/scenes.rs"
message = "Use arrow keys and first letter navigation to select a TTS engine. Arrow through as fast as you like: the voice list is reloaded a moment after you stop, or straight away when you tab out of this box, so passing over engines on the way costs nothing."

[[control]]
id = "dialog.ttsSource.googleEffect"
label = "Google Cloud audio effects profile"
file = "src/ui/scenes.rs"
message = "Optionally optimize Google Cloud speech for a playback device, such as headphones, a telephone, a Bluetooth speaker, a car, or a home entertainment system. No effects profile omits the parameter."

[[control]]
id = "dialog.ttsSource.googleLanguage"
label = "Google Cloud language code"
file = "src/ui/scenes.rs"
message = "Enter a Google Cloud language code such as en dash US. Leave blank to infer the language from the selected voice name."

[[control]]
id = "dialog.ttsSource.gttsSpeed"
label = "Google Translate speed mode"
file = "src/ui/scenes.rs"
message = "Choose the Google Translate endpoint's normal or slow speech mode. Provider default omits your preference and currently behaves as normal speed."

[[control]]
id = "dialog.ttsSource.gttsTld"
label = "Google Translate accent domain"
file = "src/ui/scenes.rs"
message = "Enter the Google domain suffix used to select a regional accent, such as co.uk or com.au. Leave blank for com. Invalid host characters are rejected before a request is sent."

[[control]]
id = "dialog.ttsSource.openaiInstructions"
label = "OpenAI voice instructions"
file = "src/ui/scenes.rs"
message = "Describe how the OpenAI voice should speak, such as warm, calm, or energetic. Instructions are sent only with GPT-4o mini TTS models and omitted for TTS 1 models."

[[control]]
id = "dialog.ttsSource.openaiModel"
label = "OpenAI speech model"
file = "src/ui/scenes.rs"
message = "Choose an OpenAI speech model discovered with your validated credentials. Saved legacy models remain selectable when unavailable."

[[control]]
id = "dialog.ttsSource.pitch"
label = "TTS voice pitch slider"
file = "src/ui/scenes.rs"
message = "Use arrows, page up, page down, home and end to set the pitch of this voice. Only Microsoft Edge, Azure, and Google Cloud support pitch; for other engines this control is announced as unsupported and has no effect."

[[control]]
id = "dialog.ttsSource.pollyEngine"
label = "Polly engine"
file = "src/ui/scenes.rs"
message = "Choose the AWS Polly synthesis engine for this source: standard, neural, long-form, or generative. Provider default omits the parameter and uses standard. Pitch is available only with standard."

[[control]]
id = "dialog.ttsSource.pollyLanguage"
label = "Polly language code"
file = "src/ui/scenes.rs"
message = "Optionally enter the Polly language code for a bilingual voice. Leave blank to let the selected voice determine its language."

[[control]]
id = "dialog.ttsSource.preview"
label = "Preview voice button"
file = "src/ui/scenes.rs"
message = "Press to hear a test phrase with the current engine, voice, rate, volume, and pitch. The preview plays only to you and never reaches the stream. If synthesis fails, the reason is shown here, which is the quickest way to check an API key."

[[control]]
id = "dialog.ttsSource.rate"
label = "TTS speech rate slider"
file = "src/ui/scenes.rs"
message = "Use arrows, page up, page down, home and end to set the rate of the speech."

[[control]]
id = "dialog.ttsSource.reset"
label = "Reset this engine to defaults button"
file = "src/ui/scenes.rs"
message = "Puts the engine currently selected above back to its default voice, volume, rate, pitch and provider settings. Only that engine is reset; every other engine keeps what you have set for it. Nothing is saved until you press OK, so cancel undoes the reset."

[[control]]
id = "dialog.ttsSource.toStream"
label = "Send speech to the stream checkbox"
file = "src/ui/scenes.rs"
message = "When this is checked, speech is mixed into the outgoing stream so your listeners hear it. When it is not, the speech reaches neither the stream nor any bus this source sends to. Either way you hear it yourself: speech is always played to you, whichever engine it comes from, and you do not need to monitor this source to hear it."

[[control]]
id = "dialog.ttsSource.voice"
label = "TTS voice choice"
file = "src/ui/scenes.rs"
message = "Use arrow keys and first letter navigation to choose the tts voice for this engine. This box reports how many voices are available, or says they have not been fetched yet; the same line appears below it. Default voice leaves the choice to the engine."

[[control]]
id = "dialog.ttsSource.volume"
label = "TTS voice volume slider"
file = "src/ui/scenes.rs"
message = "Use arrows, page up, page down, home and end to set the volume of this voice."

[[control]]
id = "dialog.update.cancel"
label = "Cancel the update download button"
file = "src/ui/update_dialog.rs"
message = "Stops the download and closes this window. Nothing on your computer has been changed at this point, so cancelling is always safe; the part that was downloaded is thrown away. You can start the update again later from the General tab of Preferences."

[[control]]
id = "dialog.update.progress"
label = "Update download progress bar"
file = "src/ui/update_dialog.rs"
message = "How much of the update has been downloaded so far, as a percentage. It reaches one hundred percent when the download is complete and Pubsplash starts checking it."

[[control]]
id = "dialog.update.status"
label = "Update download status text"
file = "src/ui/update_dialog.rs"
message = "How far the download has got, in megabytes. This is a read-only box you can tab to and read whenever you like; it does not read itself out as it changes, so it won't talk over you while you work."

[[control]]
id = "tab.api.refreshButton"
label = "Refresh account balances button"
file = "src/ui/api.rs"
message = "Ask each provider that publishes one how much credit is left on your account. Only ElevenLabs reports a balance; the others are marked unavailable. This is never done automatically, so pressing it is the only thing that spends a request on your behalf."

[[control]]
id = "tab.api.usageList"
label = "Per-engine API usage list"
file = "src/ui/api.rs"
message = "What each speech engine has been asked to do since Pubsplash started, most recently used engine first. Only engines that have actually spoken appear. Each engine's name is followed by its indented figures: requests sent, characters sent, credits spent, remaining balance, models used, voices used, and failures. Anything the provider does not report reads as unavailable. Use arrows, page up, page down, home, and end to navigate this list."

[[control]]
id = "tab.buses.busAdd"
label = "Add bus button"
file = "src/ui/buses.rs"
message = "Create a new bus. Buses hold effects and output to master. You decide what sources send their output to which buses,, how much of their signal to send, and whether they should output to master as well as to the bus."

[[control]]
id = "tab.buses.busDown"
label = "Move bus down button"
file = "src/ui/buses.rs"
message = "Move the selected bus down in the list. CTRL+Down will also work as long as focus is on the list of buses."

[[control]]
id = "tab.buses.busList"
label = "Buses list (row 0 is the master output)"
file = "src/ui/buses.rs"
message = "This is the list of buses, use arrow keys, page up, page down, home, and end to select one to work with. Use CTRL+arrows to move the selected bus up or down in the list."

[[control]]
id = "tab.buses.busRemove"
label = "Remove bus button"
file = "src/ui/buses.rs"
message = "Deletes the selected bus. Note that you cannot delete master."

[[control]]
id = "tab.buses.busRename"
label = "Rename bus button"
file = "src/ui/buses.rs"
message = "Renames the selected bus."

[[control]]
id = "tab.buses.busUp"
label = "Move bus up button"
file = "src/ui/buses.rs"
message = "Move the selected bus up in the list. CTRL+up will also work as long as focus is on the list of buses."

[[control]]
id = "tab.buses.chainExport"
label = "Export FX chain file button"
file = "src/ui/buses.rs"
message = "Export the selected FX chain to a file where it can be imported for use on another machine."

[[control]]
id = "tab.buses.chainImport"
label = "Import FX chain file button"
file = "src/ui/buses.rs"
message = "Select an FX chain file to add to your master list of chains. It will then become usable."

[[control]]
id = "tab.buses.chainLoad"
label = "Load FX chain from library button"
file = "src/ui/buses.rs"
message = ""

[[control]]
id = "tab.buses.chainSave"
label = "Save FX chain to library button"
file = "src/ui/buses.rs"
message = ""

[[control]]
id = "tab.buses.fxAdd"
label = "Add plugin to chain button"
file = "src/ui/buses.rs"
message = "Add a plugin to the FX chain for the selected bus. Plugins are processed from the top down, so order matters."

[[control]]
id = "tab.buses.fxBypass"
label = "Bypass selected plugin checkbox"
file = "src/ui/buses.rs"
message = ""

[[control]]
id = "tab.buses.fxDown"
label = "Move plugin down in chain button"
file = "src/ui/buses.rs"
message = "Move the selected plugin down in the selected chain. CTRL+Down will also work as long as focus is on the list of plugins."

[[control]]
id = "tab.buses.fxEditParams"
label = "Edit plugin parameters button"
file = "src/ui/buses.rs"
message = ""

[[control]]
id = "tab.buses.fxList"
label = "Effects chain on the selected bus"
file = "src/ui/buses.rs"
message = "This is the list of effects on the selected bus. Use arrow keys, page up, page down, home, and end to select an effect. CTRL+Up and CTRL+Down move the selected effect. Since effects process from top to bottom, order matters!."

[[control]]
id = "tab.buses.fxOpenInterface"
label = "Open plugin interface button"
file = "src/ui/buses.rs"
message = "If a plugin has an accessible interface, pressing this button will open it. From there, you can press F6 to try to focus the window and use standard controls to work with it. If you know the plugin doesn't have an accessible interface, it's better to use the edit parameters button instead."

[[control]]
id = "tab.buses.fxRemove"
label = "Remove plugin from chain button"
file = "src/ui/buses.rs"
message = "Remove the selected plugin from the FX chain."

[[control]]
id = "tab.buses.fxUp"
label = "Move plugin up in chain button"
file = "src/ui/buses.rs"
message = "Move the selected plugin up in the chain. CTRL+Up will also work as long as focus is on the list of plugins."

[[control]]
id = "tab.chat.input"
label = "Chat message input box"
file = "src/ui/chat.rs"
message = "Type a message and press ENTER to chat to your stream."

[[control]]
id = "tab.chat.messageList"
label = "Chat message list"
file = "src/ui/chat.rs"
message = "All chats you receive while streaming can be viewed here. Use arrows, page up, page down, home, and end to navigate this list."

[[control]]
id = "tab.chat.reconnectButton"
label = "Reconnect chat feed button"
file = "src/ui/chat.rs"
message = "Open a fresh connection to the chat feed without interrupting your stream. Pubsplash reconnects on its own when it notices the feed has gone quiet, so you should rarely need this; use it if chat has stopped arriving and you would rather not wait. Your audio keeps going out either way."

[[control]]
id = "tab.chat.sendButton"
label = "Send chat message button"
file = "src/ui/chat.rs"
message = "Press this button to send your message."

[[control]]
id = "tab.chat.viewButton"
label = "View selected message button"
file = "src/ui/chat.rs"
message = "View the selected chat in a box that will allow for standard text navigation, selection, and copying."

[[control]]
id = "tab.home.mixer.strip.mute"
label = "Mixer strip mute checkbox"
file = "src/ui/home.rs"
message = ""

[[control]]
id = "tab.home.mixer.strip.openFile"
label = "Mixer strip open file button"
file = "src/ui/home.rs"
message = "Plays one file of your choosing on this media player. The file can be anywhere on your computer and does not have to be in the source's folder. It interrupts whatever is playing, exactly as skipping to the next track does, and when it finishes the folder carries on with the track it was going to play next. Nothing about the file is remembered. This button only appears on a media player's strip."

[[control]]
id = "tab.home.mixer.strip.volume"
label = "Mixer strip volume slider"
file = "src/ui/home.rs"
message = "Sets this strip's level, from 0 to 100 percent, where 100 is unity gain. Up, right, and page up raise the volume; down, left, and page down lower it. Arrows step by 1 percent and page keys by 10. Home jumps to the maximum and end to silence. If a source is too quiet even at 100 percent, open the context menu with shift plus F10, the applications key, or a right click, and choose Enable volume boost: the slider then goes up to 500 percent, amplifying the signal. Turning volume boost back off snaps the slider down to 100 percent."

[[control]]
id = "tab.home.overview"
label = "Stream overview list"
file = "src/ui/home.rs"
message = "A read-only list of the current state of your broadcast, one fact per row. The status row is always there and tells you whether you're streaming, recording, or both. While you're streaming, quality, listener, and listener peak rows appear below it; quality is the bitrate and format going out to your listeners. Whenever a stream or a recording is running, a duration row shows how long it has been going. That row counts up in the background, but the row you have selected is left alone until you go to it, so nothing in this list ever reads itself out at you. It's brought up to date the moment before you'd hear it: arrow onto the row, or tab into the list, and it reads the time as it is now, once."

[[control]]
id = "tab.home.recordButton"
label = "Start/stop recording button"
file = "src/ui/home.rs"
message = "Start or stop local recording. Note: You can't use this button if you're already streaming. If you'd like to record a stream, this can be done by checking the appropriate box in File > set stream info."

[[control]]
id = "tab.home.sceneList"
label = "Scenes list"
file = "src/ui/home.rs"
message = "Select a scene and press the switch button to transition to it. The order your scenes appear is the order they're set to in the scenes tab."

[[control]]
id = "tab.home.streamButton"
label = "Start/stop streaming button"
file = "src/ui/home.rs"
message = "Starts or stops streaming to Audio Pub. Note: This button will be unavailable if you've started a local recording."

[[control]]
id = "tab.home.switchScene"
label = "Switch to scene button"
file = "src/ui/home.rs"
message = "Transition to the selected scene."

[[control]]
id = "tab.scenes.sceneAdd"
label = "Add scene button"
file = "src/ui/scenes.rs"
message = "Create a new scene which allows you to organize your sources by how you intend to use them. Scenes can be switched on the home tab. The default scene is always here and can be renamed, but not deleted."

[[control]]
id = "tab.scenes.sceneDown"
label = "Move scene down button"
file = "src/ui/scenes.rs"
message = "Move the selected scene down in the list. CTRL+Down will also work as long as focus is on the list of scenes."

[[control]]
id = "tab.scenes.sceneList"
label = "Scenes list"
file = "src/ui/scenes.rs"
message = "This is a list of configured scenes. Use the arrows, page up, page down, home, and end to navigate it. CTRL+Up and CTRL+Down will move the selected scene in the list. You transition between scenes via the list on the home tab. The order of the scenes on the home tab mirrors the order here, so configure them to suit your preference."

[[control]]
id = "tab.scenes.sceneRename"
label = "Rename scene button"
file = "src/ui/scenes.rs"
message = "Rename the selected scene."

[[control]]
id = "tab.scenes.sceneUp"
label = "Move scene up button"
file = "src/ui/scenes.rs"
message = "Move the selected scene up in the list. CTRL+Up will also work as long as focus is on the list of scenes."

[[control]]
id = "tab.scenes.sourceAdd"
label = "Add source button"
file = "src/ui/scenes.rs"
message = "Add a new source to the selected scene. A source is something your stream will hear, such as your microphone, an app, a tts engine, etc."

[[control]]
id = "tab.scenes.sourceDown"
label = "Move source down button"
file = "src/ui/scenes.rs"
message = "Move the selected source down in the list. CTRL+Down will also work as long as focus is on the list of sources."

[[control]]
id = "tab.scenes.sourceEdit"
label = "Edit source button"
file = "src/ui/scenes.rs"
message = "Edit this source; what this does depends on the source. If it's a microphone source, it sets the mic to use, if it's a tts source, it sets the voice and parameters."

[[control]]
id = "tab.scenes.sourceList"
label = "Sources list for the selected scene"
file = "src/ui/scenes.rs"
message = "Use the arrow keys, page up, page down, home, and end to select a source to work with. CTRL+Up and CTRL+Down can be used to move the selected source in the list."

[[control]]
id = "tab.scenes.sourceRemove"
label = "Remove source button"
file = "src/ui/scenes.rs"
message = "Delete the selected source."

[[control]]
id = "tab.scenes.sourceSends"
label = "Source sends button"
file = "src/ui/scenes.rs"
message = "Set up one or more buses you'd like to send this source's signal to. This is how you add effects. You send the source to a bus, put the effects on the bus and voila!"

[[control]]
id = "tab.scenes.sourceUp"
label = "Move source up button"
file = "src/ui/scenes.rs"
message = "Move the selected source up in the list. CTRL+Up will also work as long as focus is on the list of sources."

[[control]]
id = "window.tabBar"
label = "Main tab bar"
file = "src/ui/mod.rs"
message = "The tabs of the main window: Home, Chat, Scenes and Sources, and Buses. Use the left and right arrow keys to move between tabs, then TAB to go into the tab's contents. From anywhere in a tab, F6 jumps forward to the next list, and on past the last one back here to the tabs; SHIFT+F6 goes the other way."

[[stale]]
id = "dialog.soundEventsSource.browse"
label = "Browse for a sound pack button"
file = "src/ui/scenes.rs"
message = "Opens a file dialog to pick the compiled sound pack, a dot pspack file, that this source plays its sounds from."

[[stale]]
id = "dialog.soundEventsSource.packPath"
label = "Sound pack path input"
file = "src/ui/scenes.rs"
message = "The sound pack this source plays from. Type the full path to a compiled dot pspack file, or to a sound pack project folder if you are still working on one, or use the browse button. Until this is filled in, the source stays silent."

[[stale]]
id = "dialog.ttsSource.fetchVoices"
label = "Get available voices button"
file = "src/ui/scenes.rs"
message = "Voice and model lists refresh automatically at startup and after successful credential validation. Pubsplash keeps the last successful cached list if a provider cannot be reached."
